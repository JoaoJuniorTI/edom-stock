// Glass effect applied inline — immune to PostCSS/Tailwind purge
export const glass = {
  card: {
    background: 'rgba(255,255,255,0.78)',
    backdropFilter: 'blur(32px) saturate(200%) brightness(1.02)',
    WebkitBackdropFilter: 'blur(32px) saturate(200%) brightness(1.02)',
    border: '1px solid rgba(255,255,255,0.92)',
    borderRadius: '22px',
    boxShadow: '0 4px 20px rgba(0,0,0,0.08), 0 1px 0 rgba(255,255,255,0.95) inset, 0 -0.5px 0 rgba(0,0,0,0.04) inset',
    position: 'relative' as const,
    overflow: 'hidden' as const,
  },
  kpi: {
    background: 'rgba(255,255,255,0.80)',
    backdropFilter: 'blur(36px) saturate(220%) brightness(1.03)',
    WebkitBackdropFilter: 'blur(36px) saturate(220%) brightness(1.03)',
    border: '1px solid rgba(255,255,255,0.95)',
    borderRadius: '28px',
    boxShadow: '0 4px 20px rgba(0,0,0,0.08), 0 1px 0 rgba(255,255,255,0.95) inset',
    padding: '24px 26px 22px',
    cursor: 'pointer' as const,
    position: 'relative' as const,
    overflow: 'hidden' as const,
    transition: 'all 0.25s cubic-bezier(0.16,1,0.3,1)',
    userSelect: 'none' as const,
  },
  sidebar: {
    background: 'rgba(235,235,240,0.90)',
    backdropFilter: 'blur(48px) saturate(220%)',
    WebkitBackdropFilter: 'blur(48px) saturate(220%)',
    borderRight: '1px solid rgba(255,255,255,0.75)',
    boxShadow: '2px 0 32px rgba(0,0,0,0.06)',
  },
  input: {
    background: 'rgba(255,255,255,0.86)',
    backdropFilter: 'blur(8px)',
    WebkitBackdropFilter: 'blur(8px)',
    border: '1px solid rgba(0,0,0,0.12)',
    borderRadius: '12px',
    outline: 'none',
    width: '100%',
    fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Text", sans-serif',
    fontSize: '14px',
    fontWeight: 400,
    padding: '11px 15px',
    color: '#1C1C1E',
    boxShadow: '0 1px 3px rgba(0,0,0,0.05) inset',
    transition: 'all 0.18s ease',
  },
}
